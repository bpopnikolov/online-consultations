import { HighlightMsgDirective } from './highlight-msg.directive';

describe('HighlightMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMsgDirective();
    expect(directive).toBeTruthy();
  });
});
